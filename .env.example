# Server-only. Never expose this as NEXT_PUBLIC_*.
PLANE_API_KEY=
PLANE_PROJECT_ID=
PLANE_WORKSPACE_SLUG=
PLANE_BASE_URL=https://prosjekt.example.com

# Optional server-only Slack alert. Never expose this as NEXT_PUBLIC_*.
FEEDBACK_SLACK_WEBHOOK_URL=

# Local dev-server only. Use any long random value.
DEV_FEEDBACK_TOKEN=
